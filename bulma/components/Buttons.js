import {Html} from '../../src'
import {Button} from '../elements'

class Buttons extends Html {
  static defaultOpts = {
    css: 'buttons',
    defaultItem: {
      as: Button
    }
  }
}

export default Buttons
