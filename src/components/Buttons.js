import {Html} from '../core'
import {Button} from '../elements'

class Buttons extends Html {
  static defaultOpts = {
    as: 'buttons',
    defaultItem: {
      type: Button
    }
  }
}

export default Buttons
